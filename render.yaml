# Deploy the HouseWatch risk engine + dashboard on Render's free tier.
services:
  - type: web
    name: housewatch
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
